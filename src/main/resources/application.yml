logging:
  file:
    path:
  level:
    sql: info  # change this to info before going live


# h2 database configuration
spring:
#---------   h2
#  datasource:
#    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
#    driverClassName: org.h2.Driver
#    username: sa
#    password:
#  jpa:
#    database-platform: org.hibernate.dialect.H2Dialect
#    generate-ddl: true
#    hibernate:
#      ddl-auto: update
#    properties:
#      hibernate:
#        format_sql: true
#  h2:
#    console:
#      enabled: true
#      path: /h2-console
#      settings:
#        web-allow-others: true

#-------  mysql

  datasource:
    url: jdbc:mysql://localhost:3306/${DATABASE_NAME}?useSSL=false&serverTimezone=CET
    username: ${DATABASE_USER}
    password: ${DATABASE_PASSWORD}
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    generate-ddl: true

  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER

 #   path: /home/gitpod/testdb



# desc info
dev-info:
  mail: PRINCE.22.REPLY@GMAIL.COM
  server-name: RANIERI_AMBIENTE
  developer: DONCHRIS
  description: GESTIONE RIFIUTI
